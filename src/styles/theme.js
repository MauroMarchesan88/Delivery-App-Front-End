import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d61a69',
      contrastText: '#fff',
    },
    white: {
      main: '#fff',
    },
  },
});

export default theme;
